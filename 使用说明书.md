## 合约交易策略终端 · 使用说明书

### 一、项目简介

本终端是一款基于 OKX 实时数据的加密货币合约（永续合约）交易辅助工具。它能在后端持续拉取行情、计算技术指标、检测策略信号并附带完整的止盈止损（SL/TP）建议。前端提供仪表盘、策略配置、回测引擎、SL/TP 计算器和交易所 API 接入六个功能面板。

**核心特点：**
- 后端 Python 服务器持续计算指标，关掉浏览器信号也不丢失
- 6 种经学术研究验证的交易策略，附带置信度评分
- 每条信号自动附带三层止盈 + 三层止损 + 仓位计算
- 前端零框架纯 JS，加载即用
- 支持部署到 Railway / Render 生产环境

**重要提示：** 本工具仅供研究和教育目的。加密货币交易风险极高，过往回测表现不保证未来收益。

---

### 二、快速启动

#### 2.1 本地运行（最简方式）

```bash
cd /Users/terry/Documents/交易策略
python3 comboserver.py
```

启动后打开浏览器访问：**http://127.0.0.1:9878/**

首次启动需要约 15 秒加载 K 线数据，请稍等片刻。

#### 2.2 生产环境地址

| 环境 | 地址 |
|------|------|
| Railway（主） | https://crypto-scanner-production-8b0c.up.railway.app/ |
| Render（备） | https://crypto-scanner-yrdu.onrender.com |

#### 2.3 重启本地服务器

```bash
lsof -ti :9878 | xargs kill -9
cd /Users/terry/Documents/交易策略 && python3 comboserver.py
```

---

### 三、系统架构

```
┌─────────────────────────────────────────────────────────┐
│                     comboserver.py                       │
│  (Python · 端口 9878 · 多线程)                            │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐  │
│  │ engine_loop  │  │ indicator    │  │ signal detect │  │
│  │ (每5s拉行情)  │→│ 布林·RSI·MACD│→│ 6策略 + SL/TP │  │
│  └──────────────┘  └──────────────┘  └───────┬───────┘  │
│                                               │          │
│  ┌──────────────────────────────────────────────┐        │
│  │  API 端点: /api/state, /api/signals,         │        │
│  │  /api/priceData, /api/stream (SSE),          │        │
│  │  /api/okx/tickers, /api/okx/candles          │        │
│  └──────────────────────────────────────────────┘        │
│  ┌──────────────────────────────────────────────┐        │
│  │  持久化: signals.json + candles_cache.json    │        │
│  └──────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│               contract-terminal.html                      │
│  (纯前端 · 6个面板 · SSE实时推送 + 30s轮询)                 │
│                                                         │
│  📊仪表盘  ⚙️策略配置  🔔信号日志  🧪回测  🎯SL/TP  🔌API │
└─────────────────────────────────────────────────────────┘
```

---

### 四、各功能面板详解

#### 4.1 仪表盘（Dashboard）

打开终端后的默认视图，包含：

| 区域 | 说明 |
|------|------|
| **行情卡片** | 8 个追踪币种的实时价格 + 24h 涨跌幅，绿涨红跌 |
| **BTC/USDT K线图** | Canvas 绘制的实时 K 线，含布林带（20,2）、EMA50、EMA200。顶部可切换 15m / 1h / 4h 时间框架 |
| **最新信号** | 最近触发的策略信号，每条含完整 SL/TP 标签 |
| **币种追踪表** | 所有币种的当前价、24h%、RSI、布林位置、MACD方向、EMA趋势、策略信号和 SL/TP 建议 |

顶栏控件：

| 控件 | 作用 |
|------|------|
| `OKX 实时` 徽章 | 显示数据源状态（绿色=在线） |
| `更新: HH:MM:SS` | 上次数据刷新时间 |
| `🔄 刷新` | 手动拉取最新数据 |
| `⏸ 自刷:开/关` | 切换 30 秒自动刷新 |
| `📡 WS开/关` | 切换 SSE 实时推送 |

#### 4.2 策略配置（Strategies）

可对 6 种策略单独开关和调整参数，修改后点 **💾 保存配置** 生效。

##### 策略一：布林带均值回归（1h）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 周期 | 20 | 布林带 SMA 周期 |
| 标准差 | 2 | 带宽倍数 |
| RSI 过滤 | 开启 | 配合 RSI 过滤假信号 |

> **研究依据：** Theseus 论文验证 BTC 在 1h 级别呈均值回归特性，年化 74.8%，夏普 1.48。

##### 策略二：RSI 超买超卖（1h）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 周期 | 14 | RSI 计算周期 |
| 超卖阈值 | 30 | RSI 低于此值考虑做多 |
| 超买阈值 | 70 | RSI 高于此值考虑做空 |
| EMA50 趋势过滤 | 开启 | 价格在 EMA50 上方仅做多，下方仅做空 |

##### 策略三：MACD 金叉死叉（4h）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 快线 | 12 | EMA 快线周期 |
| 慢线 | 26 | EMA 慢线周期 |
| 信号线 | 9 | Signal 周期 |
| EMA200 过滤 | 开启 | 大趋势过滤，顺势交易 |

##### 策略四：EMA 趋势跟踪（4h）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| EMA 快线 | 50 | 短周期均线 |
| EMA 慢线 | 200 | 长周期均线 |
| 仅回调入场 | 开启 | 金叉后等回调到 EMA50 才入场 |

##### 策略五：RBF 假突破（1h）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 区间周期 | 20 | 识别区间的 K 线数 |
| 突破幅度 | 1.5% | 突破区间边界超过此比例视为假突破 |

> **研究依据：** IndieHackers 回测 238 笔交易，56% 胜率，极端恐惧市场中少数盈利策略之一。

##### 策略六：综合多策略（1h）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 最少共振数 | 2 | 布林+RSI+MACD 中至少几个方向一致才触发 |

> 信号旁边显示策略共振详情，如 `2策略共振 B:1 R:0 M:1` 表示布林和 MACD 同时看多。

#### 4.3 信号日志（Signals）

展示最近 200 条触发的信号，含以下列：

| 列 | 说明 |
|----|------|
| 时间 | 信号触发时间（K 线时间，非发现时间） |
| 币种 | 交易对 |
| 策略 | 触发策略名称 |
| 方向 | LONG（看多）或 SHORT（看空），绿色/红色标签 |
| 入场 | 建议入场价 |
| 止损 | SL 价格及百分比 |
| TP1 | 第一止盈位（R:R 1:1.5） |
| TP2 | 第二止盈位（R:R 1:2.5） |
| R:R | 风险回报比 |
| 置信度 | 0-100+ 数值 |

信号去重：同一币种+策略+方向+K线时间的组合只提醒一次。

#### 4.4 回测引擎（Backtest）

基于历史 K 线模拟交易，评估策略表现。

**参数设置：**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 币种 | BTCUSDT | 可选 5 个主流币 |
| 策略 | 6 选 1 | 选择要回测的策略 |
| 时间框架 | 1h | 15m / 1h / 4h / 日线 |
| 天数 | 90 | 回测周期 |
| 初始资金 | $10,000 | 起始本金 |
| 手续费 | 0.05% | 每笔交易手续费 |
| 杠杆 | 5x | 1x-125x |
| 风险% | 2% | 每笔交易风险敞口 |
| 止损% | 3% | 硬止损比例 |

**输出指标：** 总收益、胜率、盈亏比、最大回撤、权益曲线图、逐笔交易明细。

> ⚠️ 回测使用前端 JavaScript 模拟数据，独立于服务器实时数据流。结果仅供参考，不包含资金费率和滑点。

#### 4.5 止盈止损计算器（SL/TP）

手动或自动计算入场后的止盈止损建议。

**输入参数：**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 币种 / 方向 / 入场价 | — | 指定交易对和方向 |
| ATR 倍数 | 1.5 | 波动率止损的 ATR 倍数 |
| 风险% | 2% | 单笔风险敞口 |
| 账户余额 | $10,000 | 用于仓位计算 |
| 杠杆 | 5x | 合约杠杆 |

**计算逻辑（三层止损取最紧）：**
1. **ATR 止损** = 入场价 ± ATR(14) × 1.5
2. **硬止损** = 入场价 × (1 ± 3%)
3. **结构止损** = 近 20 根 K 线最低/最高价

**三层止盈：**
- TP1 = 入场价 ± 风险距离 × 1.5（保守，1:1.5 R:R）
- TP2 = 入场价 ± 风险距离 × 2.5（激进，1:2.5 R:R）
- TP3 = 布林带对侧轨道 / 前高前低（结构目标）

**仓位计算公式：**
```
仓位 = (账户余额 × 风险%) / |入场价 − 止损价| × 杠杆
```

右侧面板同步显示最近 24 小时内所有活跃信号的 SL/TP 汇总。

#### 4.6 交易所 API（Exchange）

接入真实交易所实现自动/手动下单。

**支持的交易所：**
- Binance Futures（U 本位永续）
- OKX（U 本位永续）
- Bybit（U 本位永续）

**配置步骤：**
1. 选择交易所
2. 填入 API Key + Secret Key（OKX 还需 Passphrase）
3. 点击 **🔗 连接测试** 验证凭据
4. 点击 **💾 保存** 将配置加密存入浏览器本地存储

> ⚠️ 仅需只读+交易权限，**切勿授予提现权限**。API 密钥只存浏览器本地，不上传到服务器。

**自动交易：** 勾选「启用自动交易」并选择要跟单的策略后，策略信号触发时将自动以市价单执行。建议先在回测中验证策略后再开启。

---

### 五、数据与指标说明

#### 5.1 数据源

| 数据 | 来源 | 更新频率 |
|------|------|---------|
| 实时价格 | OKX REST API `/api/v5/market/tickers` | 每 5 秒 |
| 历史 K 线 | OKX REST API `/api/v5/market/history-candles` | 启动时加载，后续 ticker 带动更新 |
| 信号推送 | 服务器 SSE `/api/stream` | 实时 |

#### 5.2 追踪币种

BTCUSDT · ETHUSDT · SOLUSDT · DOGEUSDT · BNBUSDT · AVAXUSDT · LINKUSDT · ARBUSDT

全部对应 OKX 永续合约（`-USDT-SWAP`）。

#### 5.3 技术指标

| 指标 | 计算方式 | 用途 |
|------|---------|------|
| **布林带 (20,2)** | SMA20 ± 2σ | 判断超买超卖、波动率范围 |
| **RSI (14)** | Wilder's 平滑 | 0-100 动量指标，<30 超卖 >70 超买 |
| **MACD (12,26,9)** | EMA 差值 | 趋势方向和动量变化 |
| **EMA50 / EMA200** | 指数移动平均 | 中长期趋势判断 |
| **ATR (14)** | 真实波幅均值 | 波动率度量，用于止损计算 |

#### 5.4 信号去重

每条信号由 `币种_策略_方向_K线时间` 组成唯一键，去重后存于 `notified_keys.json`。服务器重启后已通知的信号不会重复推送。

---

### 六、环境变量与部署

#### 6.1 本地开发

| 组件 | 端口 | 启动命令 |
|------|------|---------|
| comboserver.py | 9878 | `python3 comboserver.py` |

Python 3.11+，标准库零依赖。

#### 6.2 Railway 部署

配置文件：
- `Procfile`: `web: python comboserver.py`
- `runtime.txt`: `python-3.11.0`
- `requirements.txt`: 无外部依赖

部署方式：推送到 GitHub 主分支，Railway 自动构建部署。

#### 6.3 Render 部署

配置文件：`render.yaml`

```yaml
services:
  - type: web
    name: crypto-scanner
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python comboserver.py
    plan: free
    healthCheckPath: /api/okx/ping
```

#### 6.4 健康检查

```bash
curl http://127.0.0.1:9878/api/okx/ping
# 返回: {"pong": true, "tickers": 8, "signals": 15}
```

`tickers` 应返回 8（所有币种），`signals` 为历史信号数量。

---

### 七、常见问题

**Q: 页面一直显示「等待连接...」**
A: 检查服务器是否启动（`python3 comboserver.py`），确认终端无报错。若部署在 Railway/Render，等待约 15 秒冷启动。

**Q: 价格显示 $0.00**
A: 运行 `curl http://127.0.0.1:9878/api/okx/ping`，若 `tickers` 为 0 说明 OKX API 未响应，检查网络。通常等待几秒后自动恢复。

**Q: K 线图为空白**
A: K 线数据加载需要约 15 秒（首次启动时在后台线程拉取）。刷新页面即可。

**Q: 信号不更新**
A: 确认顶栏「自刷:开」状态和「WS开」状态均为绿色。可手动点「🔄 刷新」。

**Q: 数据看起来不是实时的**
A: 如果 OKX API 不可达，服务器不会降级到模拟数据（会显示旧数据或空值）。检查网络连接。

**Q: 如何重置所有信号？**
A: 删除 `signals.json` 和 `notified_keys.json` 后重启服务器。

---

### 八、文件清单

| 文件 | 说明 |
|------|------|
| `comboserver.py` | 主服务器：行情拉取 + 指标计算 + 信号检测 + API |
| `contract-terminal.html` | 前端单页应用（6 个面板） |
| `okx-proxy.py` | 轻量 OKX 代理（早期版本，已被 comboserver 替代） |
| `signals.json` | 信号持久化存储 |
| `notified_keys.json` | 已通知信号去重键集合 |
| `candles_cache.json` | K 线数据缓存（启动加载，加速初始化） |
| `render.yaml` | Render 部署配置 |
| `Procfile` | Railway 部署配置 |
| `runtime.txt` | Python 版本声明 |
| `requirements.txt` | Python 依赖（当前无外部依赖） |
| `test.html` | API 连通性测试页 |
| `cheatsheet.md` | 速查表 |
| `research-notes.md` | 策略研究笔记（学术验证来源） |
| `README.md` | 项目概述 |

---

### 九、免责声明

⚠️ **本工具仅供研究和教育目的。**

- 加密货币合约交易风险极高，可能导致全部本金损失。
- 所有策略信号均为基于历史数据的统计推断，**不构成任何投资建议**。
- 过往回测表现不保证未来收益。没有任何策略被独立多方验证能在多个市场周期持续盈利。
- 实盘交易前请充分理解每项策略的逻辑和局限性。
- 使用者需自行承担全部交易风险。
